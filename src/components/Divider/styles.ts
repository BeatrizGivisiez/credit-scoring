import { SxProps } from "@mui/material";

import PALETTE from "@/styles/_palette";

export const divider__box: SxProps = {
  margin: "0px",
  border: `1px solid ${PALETTE.SECONDARY_MAIN}`,
  display: "block"
};
