export interface EntityNotInGroupDTO {
  entityId: number;
  name: string;
  documentNumber: string;
  documentId: string;
  morada: string;
  localidade: string;
  status: true;
  created: string;
  updated: string;
  deleted: string | null;
}
