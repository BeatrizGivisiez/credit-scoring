export interface EntityDTO {
  entityId: number;
  name: string;
  documentNumber: string;
  // status: boolean;
  // created: string;
  // updated: string;
  // deleted: string | null;
  // documentId: string;
  // morada: string;
  // localidade: string;
  id?: number;
}
